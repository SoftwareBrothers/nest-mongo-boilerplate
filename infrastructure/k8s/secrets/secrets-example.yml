# To create values use: `echo -n 'secret-value' | base64` - and paste it under the data
apiVersion: v1
kind: Secret
metadata:
  name: secrets
  namespace: dev
type: Opaque
data:
  frontend_app_url: xxxxxxxxxx
