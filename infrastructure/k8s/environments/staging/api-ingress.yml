apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: api-ingress
  namespace: staging
spec:
  rules:
  - host: api-staging.skyhome.no
    http:
      paths:
        - path: /
          backend:
            serviceName: api-service
            servicePort: 3000
