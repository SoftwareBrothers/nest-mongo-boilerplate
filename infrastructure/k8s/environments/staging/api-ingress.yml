apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: api-ingress
  namespace: staging
spec:
  rules:
  - host: staging.your.app.com
    http:
      paths:
        - path: /
          backend:
            serviceName: api-service
            servicePort: 3000
